# ビルド生成物（Actions が毎回生成するのでコミットしない）
dist/

# ローカル確認用スクリーンショット
.preview/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS
.DS_Store

# サイト内検索の索引。uchuchu/build.py が毎回 static/ へ書き出す生成物。
# 実行機(mini)と作業機(MBAir)の両方が別々に生成するため、追跡していると
# 毎回 pull で衝突する（AIの鬼で 2026-08-21 に日次の commit が丸ごと止まった）。
static/search-*.json
